continue
Product.find(params[:id])
params[:id]
continue
@post
@post.to_json(only: [:title, :description, :id],include: [orders: { only: [:name] }],)
   34:         )
   33:           include: [orders: { only: [:name] }],
   32:           only: [:title, :description, :id],
@post.to_json(
